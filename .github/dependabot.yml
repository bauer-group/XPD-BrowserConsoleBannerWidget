# =============================================================================
# Dependabot Configuration
# =============================================================================

version: 2
updates:
  # ---------------------------------------------------------------------------
  # GitHub Actions
  # ---------------------------------------------------------------------------
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "sunday"
      time: "06:30"
      timezone: "Etc/UTC"
    labels:
      - "dependencies"
      - "github-actions"
      - "dependabot"
    commit-message:
      prefix: "chore(ci)"
    groups:
      github-actions:
        patterns:
          - "*"

  # ---------------------------------------------------------------------------
  # npm (build + deploy + test tooling)
  # ---------------------------------------------------------------------------
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "sunday"
      time: "06:30"
      timezone: "Etc/UTC"
    labels:
      - "dependencies"
      - "npm"
    commit-message:
      prefix: "chore(deps)"
    groups:
      build-tooling:
        patterns:
          - "esbuild"
      test-tooling:
        patterns:
          - "vitest"
          - "@vitest/*"
          - "jsdom"
      deploy-sdk:
        patterns:
          - "@aws-sdk/*"
          - "cloudflare"
      dev-tooling:
        patterns:
          - "eslint*"
          - "@eslint/*"
          - "typescript"
          - "typescript-eslint"
          - "globals"
          - "prettier*"
          - "husky"
          - "lint-staged"
          - "rimraf"

  # ---------------------------------------------------------------------------
  # npm (React package — @bauer-group/console-security-banner-react)
  # ---------------------------------------------------------------------------
  - package-ecosystem: "npm"
    directory: "/packages/react"
    schedule:
      interval: "weekly"
      day: "sunday"
      time: "06:30"
      timezone: "Etc/UTC"
    labels:
      - "dependencies"
      - "npm"
      - "react-package"
    commit-message:
      prefix: "chore(deps)"
    groups:
      react:
        patterns:
          - "react"
          - "react-dom"
          - "@types/react"
          - "@testing-library/*"
      build-tooling:
        patterns:
          - "tsup"
          - "typescript"
      test-tooling:
        patterns:
          - "vitest"
          - "jsdom"
          - "rimraf"
